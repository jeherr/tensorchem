name: test
channels:
dependencies:
    # Base depends
  - python
  - pip
    # Scripts
  - numpy
  - pytorch
    # Testing
  - pytest
  - pytest-cov
  - codecov
    # Pip-only installs
  #- pip:
  #  - codecov

