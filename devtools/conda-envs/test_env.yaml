name: test
channels:
dependencies:
    # Base depends
  - python
  - pip
    # Scripts
  - numpy
  - pytorch
  - ase
  - rdkit
  - networkx
    # Testing
  - pytest
  - pytest-cov
  - codecov
    # Pip-only installs
  #- pip:
  #  - codecov

